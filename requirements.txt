# The Commissioner's Desk

# Web app
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9      # required for HTML form parsing

# Data + generation
requests>=2.31
supabase>=2.28,<3
anthropic>=0.34
markdown>=3.5
python-dotenv>=1.0

# Payments. Pinned to 15.x because the paywall depends on a shape that CHANGED
# in a recent API version: current_period_end moved off the Subscription and
# onto the subscription item. See web/billing.py.
stripe>=15.0,<16

# Sign in with Google. Used for exactly one thing: verifying the signature,
# issuer and expiry of an ID token against Google's public keys. Everything
# else in the flow is two plain HTTP calls — see web/oauth.py.
google-auth>=2.40,<3

# Tests
pytest>=7.4
httpx>=0.27                  # FastAPI's TestClient depends on this
